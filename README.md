# yo-react

## 用法
```sh
// 1. git clone + npm link
git clone https://github.com/if12/generator-yo-react.git
npm link

// 2. npm global install
npm install -g generator-yo-react
mkdir react-test
cd react-test
yo yo-react --port 8080
```
